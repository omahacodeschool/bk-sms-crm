<h1>Conversation Threads</h1>

<!-- Needs carrots for Ruby code, after testing:-->

@touches.where (business_id == Business.find_by_id(business_id).user_id == @current_user.id) each do |x|
x.message
if x.read == false
x.read = true
end
end
